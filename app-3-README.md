# App 3 — Java Game Placeholder

Archive: [`app-3.zip`](app-3.zip)

App 3 currently contains a minimal `apps.Game` Java class. It compiles successfully but does not yet have a `main` method or playable game logic.

## Build

```bash
unzip app-3.zip
javac -d build 3/Game.java
```

Add a `public static void main(String[] args)` method before attempting to launch it directly.
